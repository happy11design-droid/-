# このリポジトリでの必須ルール

## 株式・チャート分析の依頼を受けたとき

ユーザーが銘柄の分析を依頼した場合（「SNDKを分析して」など。チャート画像の添付の有無を問わない。画像はClaudeが `tools/market_data.py` で生成する）、**Claude自身が独自にテクニカル分析・投資判断を行ってはならない**。

必ず、このリポジトリ直下の `チャート分析.txt` に記載された手順（手順0〜手順6-2）を、省略・改変せずそのまま実行すること。特に以下を厳守する。

- **価格・テクニカル指標・株価指数・為替・金利・Fear & Greed Index・PER等は、手順1の `tools/market_data.py` でメイン側が取得する（数秒）。** WebSearchでしか取れない項目（経済指標・決算履歴・ニュース）だけを単一のサブエージェントに委譲し、メイン会話で直接WebSearchを実行しないこと。検索結果の生データがメインの文脈を3万トークン以上消費するため。サブエージェントへの指示は `tools/データ収集指示.md` を読ませるだけにする。あわせて、サブエージェント起動と同時に、NotebookLM認証（手順A）と手順5-1・5-2の画像差し替えを並行実行すること。
- **逆に、ノートブックごとにサブエージェントを起動してはならない。** `nlm` を数本叩くだけの処理に1件あたり約57,000トークンかかり、6件で約34万トークンを浪費する（実測値）。手順5は `tools/run_notebooks.sh` による並列シェル実行で行うこと。
- 分析対象データ（画像・経済指標・ファンダメンタルズ情報）を集めた後は、`tools/build_prompt.sh` で `送信プロンプト雛形.txt` にデータを差し込み、`tools/run_notebooks.sh` でNotebookLMへ送信して、その回答を全文そのまま表示すること（例外: 監視銘柄の自動分析Routineでは全文をObsidianに保存し、チャットには判定の要約のみを出す。`監視銘柄の自動分析.txt` 手順M-3）。雛形の文言を差し込み以外の箇所で改変してはならない。
- **NotebookLMへ送信する前に、必ず `チャート分析.txt` 手順3-2の検証ゲート（全6項目）を実行すること。** 特に、直近の大きな値動きの原因を銘柄固有の事実（指数組入れ・決算・格上げ等）で説明できない状態や、チャート画像の最終日が直近取引日と一致していない状態で送信してはならない。著者ペルソナが不完全な前提で投資判断を下す原因になる。
- 取得したデータは手順3-1に従い「■取得データサマリー」として必ずユーザーへ表示すること。プロンプトに埋め込んだだけで表示しないのは不可。
- Claude自身の言葉でチャートを分析・要約して回答することは禁止。あくまでNotebookLM（各ノートブックにソースとして登録された書籍・マニュアルの著者ペルソナ）の回答を取得し、それを提示することが目的。
- Claude独自の見解を述べてよいのは、以下の3箇所のみ。
  - 手順6「■Claudeの総合見解」（複数ノートブックの回答を横断比較した上での見解）
  - 手順6-2「■リスク管理数値」（`投資憲法.md` 第3条に基づく損切り中央値・利確中央値・ポジション上限の算出。条項ごとの照合表とGO/STOP判定は廃止済み）
  - 手順8「■週次レビュー」（過去の日次記録を読み返した振り返り・答え合わせ）
  - `相場天気予報.txt` の「■Claudeの相場観」（毎朝の相場天気予報における**市場全体＝マクロ環境のみ**についての見解）

## 相場天気予報について

毎朝の相場天気予報（`相場天気予報.txt`、平日7:00 JSTにRoutineから自動実行）では、Claude自身がマクロ相場環境を判定してよい。マクロ環境を判定する著者ノートブックが存在せず、委ねる先がないためである。ただし以下を厳守する。

- **個別銘柄の売買判断（買い／売り／様子見）を書いてはならない。** 個別銘柄の判定は常にNotebookLMの著者ペルソナが行う。
- **天気予報で述べたClaudeの相場観を、`チャート分析.txt` 手順5-3のNotebookLMプロンプトに混ぜてはならない。** プロンプトへ渡すのは数値・事実のみとし、Claudeの解釈は渡さない。著者ペルソナがマニュアルではなくClaudeの相場観に影響されることを防ぐため。
- **手順6「■Claudeの総合見解」の前提としても使わない。**
- 同日中の個別銘柄分析では、天気予報の**数値データのみ**を `チャート分析.txt` 手順2-3に従って再利用してよい（経済指標は再利用可、株価・為替・金利と個別銘柄データは必ず再取得）。再利用した項目は取得時刻とキャッシュである旨を明記すること。

## その他の依頼

- 分析結果・投資判断に対して質問・疑問・反論（「なぜ様子見なのか」「打診買いは不要だったのか」「これは買いサインではないか」等）を受けた場合は、`保存・週次レビュー・QA.txt` の手順9を実行する。**Claude自身の言葉でテクニカル分析・投資判断を行って答えてはならない。** まずどのノートブックに回答させるかをユーザーに選択させ、選択されたノートブックへ質問を送信し、各著者の回答を全文表示した後、最後に「■Claudeの見解」を簡潔に（5行以内）付す。
- チャート分析（手順0〜6-2）が終わったら、依頼を待たずに `保存・週次レビュー・QA.txt` の手順7（Obsidianへの保存）を実行する。ユーザーが「保存しないで」と言った場合のみ保存しない。
- 「週次レビュー」「今週の振り返り」等と依頼された場合は `保存・週次レビュー・QA.txt` の手順8を実行する（チャート画像は不要。手順1〜6は実行しない）。
- `投資憲法.md` はユーザー自身の売買ルールである。**Claudeが内容を推測で補完したり、依頼なく書き換えたりしてはならない。** 改善提案の提示までは可。

この指示は `チャート分析.txt` の内容そのものより優先して常に適用されること。
