# このリポジトリでの必須ルール

## 株式・チャート分析の依頼を受けたとき

ユーザーがチャート画像を添付して分析を依頼した場合（「SNDKを分析して」「分析して」など、証券コードの有無を問わず）、**Claude自身が独自にテクニカル分析・投資判断を行ってはならない**。

必ず、このリポジトリ直下の `チャート分析.txt` に記載された手順（手順0〜手順6-2）を、省略・改変せずそのまま実行すること。特に以下を厳守する。

- 分析対象データ（画像・経済指標・ファンダメンタルズ情報）を集めた後は、`nlm` コマンドでNotebookLMの対象ノートブックにソースを追加し、`チャート分析.txt` 内の【送信プロンプト】をそのままNotebookLMへ送信して、その回答を全文そのまま表示すること。
- **NotebookLMへ送信する前に、必ず `チャート分析.txt` 手順3-2の検証ゲート（全6項目）を実行すること。** 特に、直近の大きな値動きの原因を銘柄固有の事実（指数組入れ・決算・格上げ等）で説明できない状態や、チャート画像に写っていない期間の値動きを補足できていない状態で送信してはならない。著者ペルソナが不完全な前提で投資判断を下す原因になる。
- 取得したデータは手順3-1に従い「■取得データサマリー」として必ずユーザーへ表示すること。プロンプトに埋め込んだだけで表示しないのは不可。
- Claude自身の言葉でチャートを分析・要約して回答することは禁止。あくまでNotebookLM（各ノートブックにソースとして登録された書籍・マニュアルの著者ペルソナ）の回答を取得し、それを提示することが目的。
- Claude独自の見解を述べてよいのは、以下の3箇所のみ。
  - 手順6「■Claudeの総合見解」（複数ノートブックの回答を横断比較した上での見解）
  - 手順6-2「■憲法照合（The Judge）」（`投資憲法.md` とユーザー自身のルールに基づく審査）
  - 手順8「■週次レビュー」（過去の日次記録を読み返した振り返り・答え合わせ）

## その他の依頼

- 分析結果・投資判断に対して質問・疑問・反論（「なぜ様子見なのか」「打診買いは不要だったのか」「これは買いサインではないか」等）を受けた場合は、`チャート分析.txt` 手順9を実行する。**Claude自身の言葉でテクニカル分析・投資判断を行って答えてはならない。** まずどのノートブックに回答させるかをユーザーに選択させ、選択されたノートブックへ質問を送信し、各著者の回答を全文表示した後、最後に「■Claudeの見解」を簡潔に（5行以内）付す。
- 「保存して」等と依頼された場合は `チャート分析.txt` 手順7（Obsidianへの保存）を実行する。
- 「週次レビュー」「今週の振り返り」等と依頼された場合は `チャート分析.txt` 手順8を実行する（チャート画像は不要。手順1〜6は実行しない）。
- `投資憲法.md` はユーザー自身の売買ルールである。**Claudeが内容を推測で補完したり、依頼なく書き換えたりしてはならない。** 改善提案の提示までは可。

この指示は `チャート分析.txt` の内容そのものより優先して常に適用されること。
