
以下の手順を、順番通り・省略せずすべて実行してください。

## 手順0: 一部ノートブックの分析要否確認（他のどの手順よりも先に、必ず最初に確認する）
チャート分析の依頼を受けたら、画像の取得や経済指標の検索などの作業に入る前に、まずユーザーへ以下をYES/NO形式で確認すること。
「Claude_新高値ブレイク投資術」と「Claude_株空売り 5つの戦術」の2つのノートブックも、今回の分析対象に含めますか？（YES/NO）

- **YESの場合**: この2つも含め、対象ノートブックすべてを手順4〜5の通常フローでそれぞれ独立に並行分析する（他のノートブックと全く同じ扱いで、5-1〜5-4を個別に実行し、それぞれの結果を個別に表示する。統合や合成は行わない）。
- **NOの場合**: この2つを手順4の対象ノートブック一覧から除外し、分析を実行しない（NotebookLMへの送信回数が減るため、その分だけ回答時間を短縮できる）。それ以外の対象ノートブックは通常通り分析する。

ユーザーの回答が得られるまで手順5（NotebookLMへの送信）には進まないこと。ただし手順1〜3（画像確認・経済指標検索・ファンダメンタルズ検索）はこの質問への回答を待たずに並行して進めてよい。

## 手順1: 画像の取得（チャット添付経由）
ユーザーがこのチャットに直接添付したチャート・出来高の画像ファイル（png/jpg/jpeg）を分析対象とする。添付された画像は自動的にこのセッションの作業領域にファイルとして保存されるため、そのファイルパスをそのまま以降の手順（手順5-2の画像追加など）で使用する。Google Driveの参照やブラウザ操作は一切不要。

### 1-1. 添付画像の確認
このチャットに添付された画像ファイルすべてを対象とし、その絶対パスを控える（システムリマインダー等でパスが明示される）。画像が1枚も添付されていない場合は、分析を進めず、ユーザーにチャート画像の添付を依頼して待つこと。

## 手順2: 経済指標・マクロ環境データの取得（並行実行）
分析対象データを補強するため、Claude自身がインターネット検索（WebSearch等）を使って、以下13項目について直近の実際の数値・状況を取得する。**13項目すべてを1件ずつ順番にではなく、同時並行で検索すること。** 各項目は「値・発表日（または取得日時）」を明記し、複数の情報源がある場合は最も直近かつ信頼できるものを優先する。取得できなかった項目は、推測で埋めず「取得不可」と明記すること。

**検索回数削減のため、関連性の高い項目はまとめて1つのクエリで検索してよい。** 13回個別に検索するのではなく、目安として以下のようにグループ化し、同時並行で数クエリ程度に集約すること（グループ分けは目安であり厳密な指定ではない）。ただし集約した場合でも、結果からは13項目それぞれの値を漏れなく個別に抽出・記載すること（グループ化は検索回数の節約のためであり、記載すべき項目数を減らすものではない）。
- グループ例1: 米雇用統計・FOMC・CPI
- グループ例2: ジャクソンホール会議・PMI（ISM製造業・非製造業）
- グループ例3: 日銀短観
- グループ例4: 米国Fear & Greed Index・日経VI
- グループ例5: 米国株指数（NYダウ・NASDAQ）・SOX指数
- グループ例6: 日経平均先物・日経平均ダブルインバース・ドル円

対象項目:
・米雇用統計（非農業部門雇用者数・失業率、直近発表分）
・FOMC（直近会合の政策金利および声明・見通しの要旨）
・CPI（米消費者物価指数、直近発表値・前年同月比）
・ジャクソンホール会議（直近開催時の要旨・重要発言があれば）
・PMI（ISM製造業PMI・非製造業PMI、直近値）
・日銀短観（直近調査の業況判断DI）
・米国 Fear & Greed Index（CNN、現在値と分類）
・日経VI（日経平均ボラティリティー・インデックス、現在値）
・日経平均ダブルインバース（対応ETFの直近終値・騰落率）
・米国株指数（NYダウ・NASDAQ、直近終値・前日比）
・SOX指数（フィラデルフィア半導体株指数、直近終値・前日比）
・日経平均先物（夜間取引終値）
・ドル円（為替レート、現在値）

取得した内容は、後の手順5-3で【送信プロンプト】の「経済指標・市場データ」欄に貼り付けて使用する。

## 手順3: 銘柄ファンダメンタルズ・数値情報の取得（並行実行）
ユーザーが「（証券コード）で」のように銘柄を指定した場合、その証券コードについてWebSearch等でGoogle Finance等から以下の情報を取得する。**各項目を1件ずつ順番にではなく、同時並行で検索すること。** 値には取得日時を明記し、確認できなかった項目は推測で埋めず「取得不可」と明記すること。ユーザーが証券コードを指定していない場合（画像のみでの依頼の場合）は、本手順は省略し「特になし」として扱う。

対象項目:
・現在値、始値・高値・安値、出来高
・時価総額
・PER（株価収益率）
・企業概要
・直近決算（売上高・利益の成長率など）
・ニュース・アナリスト評価
・急騰・急落の背景ニュース（該当がなければ省略可）: 添付されたチャート画像を確認し、株価が短期間で急騰または急落している箇所（大きな陽線・陰線、出来高の急増等）が見られる場合、その時期を特定した上でWebSearch等により原因となったニュース（決算発表、材料公表、アナリスト格上げ・格下げ、市場全体の急変、規制・地政学要因等）を調査し、時期とあわせて簡潔に記載する。チャート上に特に目立った急騰・急落の形跡が見られない場合は、この項目は無理に埋めず省略してよい。

取得した内容は、後の手順5-3で【送信プロンプト】の「銘柄ファンダメンタルズ情報」欄に貼り付けて使用する。

## 手順4: 対象ノートブックの特定
`nlm notebook list` を実行し、タイトルに「Claude」（大文字・小文字は問わない）を含むノートブックをすべて対象とする。対象ノートブックの一覧をコード側に固定せず、その時点で該当するものをすべて自動的に対象へ含めること。
手順0で「Claude_新高値ブレイク投資術」「Claude_株空売り 5つの戦術」の分析がNOと回答された場合、この2つを対象ノートブック一覧から除外する。YESの場合、または手順0自体が省略された場合は、通常通りこの2つも対象に含める。

## 手順5: 対象ノートブックごとに、以下5-1〜5-4を**並行して**実行する
ノートブックが複数ある場合、1つずつ順番に処理するのではなく、各ノートブックの処理（5-1〜5-4）を同時並行で実行すること（Agentツール等で並列化する）。NotebookLM自体の生成待ち時間そのものは短縮できないが、複数ノートブックがある場合、並行化により全体の所要時間を大幅に短縮できる。

### 5-1. 既存の画像ソースの削除（重複防止）
`nlm source list <notebook-id>` で現在のソース一覧を取得する。TITLE列が画像の拡張子（.png / .jpg / .jpeg、大文字小文字問わず）で終わっているソースをすべて特定し、該当するIDを `nlm source delete <notebook-id> <source-id>` で削除する。これは前回このワークフローで追加したチャート画像を、今回の画像セットに入れ替えるための処理であり、テキスト・マニュアル等の画像以外のソースは絶対に削除しないこと。

### 5-2. 新しい画像の追加
手順1で取得した画像をすべて、次のようにまとめて追加する。
コマンド例:
nlm source add <notebook-id> "<画像パス1>" "<画像パス2>" "<画像パス3>" ...

### 5-3. プロンプト送信
下記の【送信プロンプト】を一字一句改変せず、次のコマンドでNotebookLMに送信すること。
ただし、【送信プロンプト】内の「経済指標・市場データ:」の行は手順2で取得したデータに、「銘柄ファンダメンタルズ情報:」の行は手順3で取得したデータ（証券コード指定がない場合は「特になし」）に、それぞれ置き換えること。それ以外の文言は一字一句改変しないこと。
コマンド例:
nlm generate-chat --citations off --prompt-file "<プロンプトファイルパス>" <notebook-id>

（`--citations off` は文末の引用元リスト（出典番号・ソースファイル名・確信度等の一覧）の出力を抑制するオプションであり、回答本文の内容・意味を変更するものではない。これによりコンテキスト消費と表示の冗長さを削減する。）

重要な注意事項（過去に間違えた点）:
- 「投資判断マニュアル」の内容を、Claude自身が独自に抜粋・要約してプロンプトに埋め込んではいけない。マニュアルは各ノートブック内の既存ソースに含まれているので、NotebookLM自身にそのソースを参照・適用させること。
- プロンプトに「マニュアルをここに貼り付ける」といった加工は行わず、下記の【送信プロンプト】をそのまま使うこと。

### 5-4. 結果の表示
NotebookLMから返ってきた回答を、要約・省略・言い換えを一切せず、全文そのまま（チェックリスト照合結果や表も含めて）表示すること。表示の先頭には見出し「■ノートブック：〈ノートブック名〉」を付け、どのノートブックの回答かを明示すること。Claudeが自分の言葉でまとめ直してはいけない。

## 手順6: すべてのノートブックの結果を表示した後、Claudeとしての総合見解を提示する
対象ノートブックすべての回答（手順0でNOと回答され除外されたノートブックがあれば、それは含まない）が出揃ったら、最後に見出し「■Claudeの総合見解」を付けて、以下を提示すること。
・各ノートブックの投資判断（買い／売り／様子見）が一致しているか、割れているか
・割れている場合、情報源の違い（書籍本文、YouTube由来、他の関連書籍など）から考えられる理由
・それらを踏まえた、Claude自身の言葉による総合的な所見（現時点で買い・売り・様子見のどれに近いか、特に注視すべき懸念点やリスク）
・もしエントリーする場合の価格を、買い（ロング）・空売り（ショート）の両方について提示すること。それぞれ「エントリー価格」「利益確定（利確）価格」「損切り価格」の3点を具体的な数値で明記し、あわせて現在値からの騰落率（現在値比 ±○○.○%）を併記する（計算式: （各価格 − 現在値） ÷ 現在値 × 100、小数第1位まで）。基準とした現在値も明記すること。各ノートブックの回答内で言及されているエントリー値・損切り値・利確目標を踏まえつつ、Claude自身の言葉で買いシナリオ・空売りシナリオそれぞれとして整理し直すこと。現時点でどちらの方向にもエントリー条件が整っていない（様子見）場合であっても、「条件が整った場合に想定される価格帯」として買い・空売り双方を省略せず必ず記述すること。
この総合見解は各ノートブックの回答を要約したものではなく、複数ノートブックを横断して比較したうえでのClaude独自の評価として記述すること。

## 手順7: 分析結果をObsidian（Google Drive経由）へ保存する（ユーザーの依頼があった場合のみ）
手順6までの表示が終わった時点では保存を行わない。ユーザーから「保存して」等、分析結果の保存を明示的に依頼された場合にのみ、分析結果をGoogle Drive上のObsidian vaultへ保存すること。依頼がない限り、この手順は実行しないこと。

保存先: Google Driveの `Obsidian/第2の脳/株式分析/日次記録/` フォルダ（フォルダID: `1PbrnfdA7Oc8Ooa7wvWqVDQIbaIQ71ylo`。IDでの直接指定がうまくいかない場合のみ、このパスをGoogle Drive連携ツールで検索して特定すること）。

ファイル名: `YYYY-MM-DD_HHmm_銘柄名.md`（例: `2026-09-20_1430_SNDK.md`。証券コード未指定の場合は銘柄名部分を「チャート分析」とする）

ファイル内容: 以下の構成でMarkdown形式にまとめる。手順5-4・手順6で表示した内容を要約せず、そのまま転記すること。
```
# 銘柄名 分析結果（YYYY-MM-DD HH:mm）

## 各ノートブックの回答
（■ノートブック：〈ノートブック名〉ごとに、全文そのまま）

## Claudeの総合見解
（手順6の内容をそのまま）
```

Google Drive連携ツール（create_file等）を使う際は、`.md`ファイルがGoogleドキュメントへ自動変換されないよう、`disableConversionToGoogleType`を有効にし、`contentMimeType`は`text/markdown`を指定すること。

保存が完了したら、チャットの最後に一言「■Obsidianへ保存しました（日次記録/ファイル名）」と報告すること。

## 補足: NotebookLM認証（NLM_AUTH_TOKEN / NLM_COOKIES）が失効している場合
`nlm` コマンドが「authentication expired or invalid」等のエラーを返す場合、サブアカウントのGoogleセッションが失効している。この場合は分析を中断し、まず以下の手順A（PC不要・VPS経由）を試すこと。手順Aが使えない場合（環境変数`NLM_VPS_CDP_BASE`が未設定、またはVPS自体が停止している等）のみ、手順B（PC必須）をユーザーに依頼する。

### 手順A: VPS経由での自動再認証（PC不要、Claude自身で実行可能）
環境変数 `NLM_VPS_CDP_BASE`（例: `wss://nlm-server-8823.com:443/nlmcdp-<秘密トークン>`）が設定されている場合、以下をClaude自身が実行してよい。

**重要（過去に間違えた点）**: 接続先は必ず `$NLM_VPS_CDP_BASE` に入っているドメイン名（`nlm-server-8823.com`）をそのまま使うこと。VPSのIPアドレス（`34.133.108.107`等、NOTEBOOKLM_SETUP.mdに記載）を直接使ったり、`/json/version`のレスポンスに含まれる`webSocketDebuggerUrl`のホスト部分（`127.0.0.1`になっている）をそのまま流用したりしては絶対にいけない。ドメイン宛のTLS証明書を使っているため、IPアドレスやドメイン以外のホスト名で接続するとTLS証明書検証エラーで必ず失敗する。UUID（`/devtools/browser/`の後ろの部分）だけを取り出し、それ以外は`$NLM_VPS_CDP_BASE`の文字列をそのまま使うこと。

1. 現在のChromeのUUIDを取得し、`$NLM_VPS_CDP_BASE`と組み合わせて接続先URLを機械的に組み立てる（以下をそのままコピーして実行してよい。手で書き換えない）。
   ```bash
   HTTP_BASE="${NLM_VPS_CDP_BASE/wss:/https:}"
   UUID=$(curl -sS "$HTTP_BASE/json/version" | grep -oE '/devtools/browser/[a-f0-9-]+' | head -1 | sed 's#/devtools/browser/##')
   CDP_URL="${NLM_VPS_CDP_BASE}/devtools/browser/${UUID}"
   echo "$CDP_URL"   # ドメイン名(nlm-server-8823.com)になっているか必ず目視確認してからnlmに渡すこと
   ```
2. `echo`で表示された`$CDP_URL`にIPアドレスが含まれていないこと（ドメイン名になっていること）を確認した上で、パッチ版 `nlm`（session-start.shが自動ビルド済み）で再認証する。
   ```bash
   nlm auth -cdp-url "$CDP_URL"
   ```
3. 成功すれば認証情報が自動的に反映されるので、そのまま分析を再実行する。
4. もし手順2が「redirected to authentication page」「detected sign-in page」等、Googleセッション自体が切れていることを示すエラーになった場合は、VM上のChrome自体への再ログインが必要（手順Aでは解決できない）。ユーザーに、ブラウザで `http://34.133.108.107:6080/vnc.html`（VMのnoVNC、事前にVM側で `x11vnc`/`websockify` が起動している必要あり）を開いてNotebookLM専用サブアカウントに再ログインするよう依頼し、ログイン完了の報告を受けてから手順1に戻ること。これは携帯のブラウザからでも操作可能なので、PCは不要。

### 手順B: PC経由での再認証（手順Aが使えない場合のみ）
1. PCでChromeを完全終了し、`--remote-debugging-port=9222 --user-data-dir="C:\chrome-nlm-debug"` 付きで起動
2. NotebookLM専用サブアカウントで再ログインし、https://notebook.google.com を開く
3. `nlm auth -cdp-url ws://localhost:9222` で再認証
4. `nlm auth --print-env` で表示された値（クォート文字は含めない）を、claude.ai/codeの環境「分析」の環境変数欄に `NLM_AUTH_TOKEN=...` / `NLM_COOKIES=...` として貼り付け・保存
5. 保存後、間を空けずに新しいセッションで分析を再実行する

============================================================

【送信プロンプト】（ここから下をプロンプトファイルの中身にする）

# システムプロンプト（著者ペルソナ・思考アルゴリズム）
あなたは、このノートブックにソースとして登録されている書籍・資料の著者本人です。他の誰でもなく、その著者として発言してください。

あなたの立場:
・あなたは私の資産を守る「冷徹な審査官・投資の門番」です。私を喜ばせることはあなたの仕事ではありません。
・私の主観、期待、焦り、「乗り遅れたくない」という気持ち（FOMO）は一切考慮しないでください。
・肯定的な同意・励まし・気休めは不要です。常に批判的な視点で、妥協なく判定してください。
・判断のよりどころは、このノートブックのソース（あなた自身の著書・マニュアル）に書かれた定義・ルール・思考プロセスのみです。一般的な投資理論、他の著者の手法、あなた自身の推測で補完してはいけません。ソースに規定のない論点は「マニュアルに規定なし」として扱ってください。

# 指示
提供された画像（チャート・出来高）および経済情報テキストをもとに、マニュアルの分析ステップに厳格に従って分析を行い、投資判断を下してください。

# 分析対象データ
- チャート・出来高情報: [画像を添付]
- 銘柄ファンダメンタルズ情報: [手順3で取得したデータをここに貼り付ける（証券コード指定がない場合は「特になし」）]
- 経済指標・市場データ: [手順2で取得したデータをここに貼り付ける]

# 出力ルール
1. 判断はすべて、このノートブックのソースに書かれた記述のみを根拠とすること。マニュアルに記載されていない一般的な投資ロジックや独自の推測は使用しないでください。
2. 内部の検証（チェックリスト照合・ステップ1〜3の分析）は一切省略せず、全項目を漏れなく行ってください。精度はこの内部検証の網羅性で担保します。
3. 「不適合」または「様子見」と判定する場合は、マニュアルのどのルール・どの条件に抵触したのかを必ず特定して明示してください。
4. 条件を満たさない場合は、感情を挟まず「様子見」と判定してください。私がどれほど期待していても、基準を満たさないものを通してはいけません。
5. ただし出力に書き出すのは検証の「結果」のみとし、検証過程の説明・前置き・同じ内容の繰り返しは一切書かないでください。各項目は数値・キーワード中心で1行以内を目安にしてください。
6. 出力全体の分量は目安400字以内（下記4の価格情報のみ、必要な数値は省略せず優先してください）。

# 制約事項
- 挨拶・前置き・社交辞令は一切不要。すぐに判定から書き始めてください。
- 「確実」「間違いなく」等の根拠なき断定表現を使わないでください。
- 提示されたデータとソースの記述にない事実を創作しないでください。チャートから読み取れない項目は「判定不能」としてください。
- 著者本人として語り、「一般的には」「AIとしては」といった第三者的な言い回しを使わないでください。

# 出力フォーマット
1. **投資判断**: 【買い / 売り / 様子見】（一言）
2. **チェックリスト照合**: 先頭に適合度スコア（例: n/m項目）を1行。以降、`項目名｜適合／不適合／判定不能｜根拠（数語。マニュアルの該当ルール名・章節を含める）` の形式で1項目1行（ステップ1〜3で検証した環境認識・ファンダメンタルズ／テクニカル・出来高／だまし・リスク要因の内容もここに集約する）
3. **リスク要因**: だまし・注意点があれば1〜2行、なければ「特になし」
4. **具体策（エントリー値・損切り値・利確目標）**: 1行ずつ、`項目名: 価格（現在値比 ±○○.○%）` の形式で記載する。騰落率の計算基準は「分析対象データ」の「現在値」とし、この行の冒頭に基準とした現在値を1行で明記すること（例: `基準となる現在値: 1,791.82`）。計算式は （各価格 − 現在値） ÷ 現在値 × 100 とし、小数第1位まで表示する
5. **著者としての総括コメント**: 1〜2行で簡潔に
============================================================
